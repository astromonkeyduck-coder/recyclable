OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL_VISION=gpt-4o
OPENAI_MODEL_TEXT=gpt-4o-mini
NEXT_PUBLIC_DEFAULT_PROVIDER=general
NEXT_PUBLIC_APP_NAME="Is this recyclable?"

# Optional: Web Push (lock-screen reminders for home-screen installs)
# Generate with: npx web-push generate-vapid-keys
# VAPID_PUBLIC_KEY=...
# VAPID_PRIVATE_KEY=...
# NEXT_PUBLIC_VAPID_PUBLIC_KEY=...  (same as VAPID_PUBLIC_KEY, for client)
# PUSH_SEND_SECRET=...  (protect POST /api/push/send; e.g. for cron)
