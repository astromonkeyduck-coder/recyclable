# VAPID public key is designed to be in client bundles (Web Push). Omit from secret
# scanning so the build does not fail when it appears in .next output.
[build.environment]
SECRETS_SCAN_OMIT_KEYS = "VAPID_PUBLIC_KEY,NEXT_PUBLIC_VAPID_PUBLIC_KEY"
